[build]
  command = "npm run build-doc"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Note: _redirects file in public/ will be copied to dist/ and takes precedence
# The _redirects file uses 200! which means "redirect only if file doesn't exist"

