# Removed deprecated configurations:
# - strict-peer-dependencies (deprecated in npm 7+)
# - auto-install-peers (deprecated in npm 7+)
#
# These settings are no longer needed as npm handles peer dependencies differently now
